#include <iostream>
#include <fstream> 
#include <vector>
#include <string>
#include <iomanip>

//Player starting health would be 100, 3 enemy varibles randomised, always back and forth, 3 rounds total, only go to shop twice, six toal iteams, enemy health and attack gets higer each round, incorperate i into basicaly everything, everything is stat based, print results on to an outfile. 

using namespace std;

int main() {

    double numberTuff;

    cout << "What is 6 + 7?" << endl;
    cin >> numberTuff;

    if (numberTuff == 41 || numberTuff == 67) {
        cout << "Tuff boi" << endl;
    } else {
        cout << "NAH!!!!!!" << endl;
    }
    
}
